# ไฟล์ตั้งค่าความลับ
.env

# โฟลเดอร์ Library
node_modules/

# โฟลเดอร์รูปภาพ/สลิปที่ผู้ใช้รันขึ้นมา
uploads/

# โฟลเดอร์ backup ฐานข้อมูล (สร้างโดย createBackup(), มีข้อมูลจริง ห้ามเข้า git)
backend/backups/

# โฟลเดอร์ Build
dist/
build/

# ไฟล์ระบบ (Mac / Windows)
.DS_Store
Thumbs.db

# ไฟล์ tooling (Freebuff agent — project id metadata)
.freebuff/