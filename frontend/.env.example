# ⭐️ DEPLOY — URL ของ backend (ไม่มี /api ต่อท้าย ระบบเติมให้เอง)
# dev เว้นว่างได้ ระบบ fallback เป็น http://localhost:3000
# prod ตั้งเป็นโดเมนจริง เช่น:
VITE_API_URL=https://coop.dmtc.ac.th

# ⭐️ LINE LIFF ID สำหรับ auto-login/สมัครสมาชิกผ่าน LINE (เว้นว่าง = ใช้ค่า default ในโค้ด)
VITE_LIFF_ID=2010928001-YxK4Atjv
